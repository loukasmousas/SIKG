#!/bin/sh
# Husky v9+ style hook (no sourcing deprecated husky.sh)

echo "pre-commit: running lint-staged"
if command -v npx >/dev/null 2>&1; then
	npx lint-staged || exit 1
else
	echo "npx not found; skipping lint-staged"
fi
